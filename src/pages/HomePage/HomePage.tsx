import React, { FC } from 'react';
import Button from '../../components/Button';

const HomePage = () => {

    return (
        <Button 
            type="button" 
            ariaLabel="Test" 
            children="test"
        />
    );
};
export default HomePage;